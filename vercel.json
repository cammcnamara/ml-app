{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/upload", "destination": "/api/index" },
    { "source": "/train", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
